# El archivo con el token y la clave se queda en cada servidor, no en GitHub.
config.php

.DS_Store
Thumbs.db
*.log
.claude/
*.zip

# El estado del sitio es de cada servidor, como config.php.
# La regla que protege la carpeta sí viaja.
datos/*
!datos/.htaccess
